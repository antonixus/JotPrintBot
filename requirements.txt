# Telegram bot (async, Python 3.8+)
aiogram>=3.0,<4

# ESC/POS thermal printer (CSN-A2 and compatible models)
python-escpos>=3.0

# Configuration
python-dotenv>=1.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Standard library (no install needed - for reference only):
# textwrap, logging, asyncio
